# WebSocket Ingress for prod environment (us-east cluster 4977)
# Separate from Porter-managed ingress to set extended timeouts for WS paths.
# See cloud/issues/035-nginx-ws-timeout/spec.md for context.
#
# Apply: PORTER_CLUSTER=4977 porter kubectl -- apply -f cloud/k8s/ws-ingress-prod-us-east.yaml
# Delete: PORTER_CLUSTER=4977 porter kubectl -- delete ingress cloud-prod-cloud-ws
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cloud-prod-cloud-ws
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "60"
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  rules:
    - host: api.mentra.glass
      http:
        paths:
          - path: /glasses-ws
            pathType: Prefix
            backend:
              service:
                name: cloud-prod-cloud
                port:
                  number: 80
          - path: /app-ws
            pathType: Prefix
            backend:
              service:
                name: cloud-prod-cloud
                port:
                  number: 80
    - host: useastapi.mentra.glass
      http:
        paths:
          - path: /glasses-ws
            pathType: Prefix
            backend:
              service:
                name: cloud-prod-cloud
                port:
                  number: 80
          - path: /app-ws
            pathType: Prefix
            backend:
              service:
                name: cloud-prod-cloud
                port:
                  number: 80
    - host: useastapi.mentraglass.com
      http:
        paths:
          - path: /glasses-ws
            pathType: Prefix
            backend:
              service:
                name: cloud-prod-cloud
                port:
                  number: 80
          - path: /app-ws
            pathType: Prefix
            backend:
              service:
                name: cloud-prod-cloud
                port:
                  number: 80
  tls:
    - hosts:
        - api.mentra.glass
      secretName: cloud-prod-cloud-api-mentra-glass
    - hosts:
        - useastapi.mentra.glass
      secretName: cloud-prod-cloud-useastapi-mentra-glass
    - hosts:
        - useastapi.mentraglass.com
      secretName: cloud-prod-cloud-useastapi-mentraglass-com
