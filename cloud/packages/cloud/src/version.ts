export const CLIENT_VERSIONS = {
  required: "2.7.0", // Client must be at or above this version to work with this server.
  recommended: "2.7.0", // Client should be at or above this version for best experience.
};

export const CHINESE_CLIENT_VERSIONS = {
  required: "2.0.0", // Client must be at or above this version to work with this server.
  recommended: "2.0.0", // Client should be at or above this version for best experience.
};

export const SDK_VERSIONS = {
  required: "0.0.0",
  // recommended: "Fetched from npm",
};
