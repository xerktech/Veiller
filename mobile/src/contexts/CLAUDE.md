only write things in here if you REALLY have to / expose them to the whole app
if you just want to listend to events (i.e. via useEffect) and do changes based on them, the changes are probably better suited for the effects folder, (make sure to add the effect to AllEffects.tsx)
