# Credentials Directory

This directory contains sensitive signing credentials for the MentraOS mobile app.

## Files that belong here:

- `upload-keystore.jks` - Android app signing keystore

## Security Notes:

- **NEVER commit keystore files to git**
- Keep backup copies in a secure location (e.g., 1Password, encrypted storage)
- Share credentials securely with team members through encrypted channels
