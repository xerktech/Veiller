/** Shared accent colors for the teleprompter UI (matches the green icon). */
export const ACCENT = "var(--primary)"
export const ACCENT_FG = "var(--primary-foreground)"
export const ACCENT_TEXT = "var(--primary-text)"
/** Header gradient endpoints. */
export const HEADER_FROM = "var(--header-from)"
export const HEADER_TO = "var(--header-to)"
