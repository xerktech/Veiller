package com.mentra.asg_client.io.hardware.interfaces;

/** Hardware features exposed by the active device implementation. */
public enum Capability {
    RECORDING_LED,
    LED_BRIGHTNESS,
    RGB_LED,
    MCU_AUDIO,
    MCU_BATTERY
}
