<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <permission
        android:name="com.mentra.recovery.permission.HEARTBEAT"
        android:protectionLevel="signature" />
    <permission
        android:name="com.mentra.recovery.permission.CONTROL"
        android:protectionLevel="signature" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="com.mentra.recovery.permission.HEARTBEAT" />
    <uses-permission android:name="com.mentra.recovery.permission.CONTROL" />
    <uses-permission android:name="com.mentra.asg_client.permission.RECOVERY_TELEMETRY" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name=".ui.LauncherActivity"
            android:exported="true" />
        <service
            android:name=".service.RecoveryService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".service.RecoveryControlReceiver"
            android:exported="true"
            android:permission="com.mentra.recovery.permission.CONTROL">
            <intent-filter>
                <action android:name="com.mentra.recovery.ACTION_START_RECOVERY" />
                <action android:name="com.mentra.recovery.ACTION_REQUEST_DOWNGRADE" />
            </intent-filter>
        </receiver>
    </application>
    <queries>
        <package android:name="com.mentra.asg_client" />
    </queries>
</manifest>
